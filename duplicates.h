#pragma once
#include "scanner.h"
#include <vector>

// Найти группы дублирующихся файлов среди переданных записей.
// Возвращает вектор групп: каждая группа содержит >=2 файлов-дублей.
// Алгоритм:
//   1. Группируем файлы по размеру (разный размер — не дубли).
//   2. Внутри каждой группы одинакового размера итеративно делим
//      на подгруппы по хешам блоков 0, 1, 2, ...
//      Блок читается только тогда, когда есть с кем сравнивать.
std::vector<std::vector<const FileEntry*>>
findDuplicates(const std::vector<FileEntry>& files);
